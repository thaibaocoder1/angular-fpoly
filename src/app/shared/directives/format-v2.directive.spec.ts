import { FormatV2Directive } from './format-v2.directive';

describe('FormatV2Directive', () => {
  it('should create an instance', () => {
    const directive = new FormatV2Directive();
    expect(directive).toBeTruthy();
  });
});
